# Example gallery build files

This folder contains the Sphinx-Gallery build configuration for the public example gallery. It is not the main project documentation.

The source examples live in `examples/gallery/`. The generated HTML output goes under `docs/_build/`, and the generated gallery pages under `docs/gallery/`; both are ignored by Git and rebuilt by GitHub Actions.
